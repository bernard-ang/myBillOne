# Code scaffolding

## Angular (client)

To generate code, go to targeted directory then run command below:

### Generate Module

`ng g m {module-name}`

### Generate Component

`ng g c {component-name} -m={module-name} -c=OnPush`

### Generate Service

`ng g s {service-name}`

### Generate Guard

`ng g g {guard-name}`
